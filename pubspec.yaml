name: camping
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  rxdart: ^0.28.0
  flutter_screenutil: ^5.9.0
  go_router: ^14.3.0
  url_launcher: ^6.3.1
  keyboard_dismisser: ^3.0.0
  fuzzy: ^0.5.1
  package_info_plus: ^8.0.1

  cupertino_icons: ^1.0.6
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/location/taiwanCamping.json
    - lib/l10n/en_US.arb
    - lib/l10n/zh_TW.arb
